<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Bluetooth LE (scale link). Android 12+ uses the new runtime permissions;
         the legacy pair is kept for API 24-30 with maxSdkVersion so Play does not
         see them as requested on modern devices. neverForLocation matches the
         androidNeverForLocation flag passed to BleClient.initialize(). -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Location, and NOT capped at API 30. The cap belongs on the legacy Bluetooth
         permissions above, where location was only ever a proxy for BLE scanning —
         but this app also uses navigator.geolocation directly to stamp each load
         with its field position and to centre the map. Capacitor's
         BridgeWebChromeClient asks for COARSE+FINE at runtime when the WebView
         requests a position, and Android auto-denies a runtime request for any
         permission the manifest does not declare: with maxSdkVersion=30 here,
         every getCurrentPosition/watchPosition call failed silently on Android 12+.
         iOS has always had the matching NSLocationWhenInUseUsageDescription.
         BLUETOOTH_SCAN keeps neverForLocation — scan results are still not used
         to derive location. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
</manifest>
