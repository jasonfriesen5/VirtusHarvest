# Copy to keystore.properties (gitignored) and fill in real values.
# storeFile is resolved relative to native-app/android/, so an absolute path is
# safest — keep the .jks itself outside the repo.
storeFile=/absolute/path/to/virtus-upload.jks
storePassword=
keyAlias=
keyPassword=
